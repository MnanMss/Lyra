# 更新下载

下载进度只包含已经完成文件写入的字节。暂停先中止网络读取，等待当前写入与文件关闭结束，
再报告保留在 `.part` 中的实际大小；续传的 `Range` 从这个大小开始。

取消控制器在首次发布下载进度前就绪，因此续传刚开始时也可以暂停。取消会在下载结束后删除
残片；网络中断会保留已完成写入的部分。服务器忽略 `Range` 时覆盖重下，完整下载仍须通过
发布校验和后才能用于安装。
