# 侧边栏排序

在「项目」列表中，用鼠标拖动项目名称可调整同一区域的项目顺序；置顶项目与普通项目分别排序。
拖动项目内的会话可以调整该项目的会话顺序，并切换到「手动排序」。打开项目、切换会话或重启后保留排序。

「列表设置」可以切回最近更新或最近创建，再选「手动排序」恢复之前的位置。新的会话显示在手动列表顶部。
聊天列表、单独置顶的会话、无项目会话和归档列表仍按时间排列。触屏保持原生滚动。

拖动中按 Escape、失焦或收到系统取消事件都会取消移动，不修改保存的顺序。松开后只排序，不打开会话或折叠项目。
